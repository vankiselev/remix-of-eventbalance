import Layout from "@/components/Layout";
import Events from "@/components/Events";

const EventsPage = () => {
  return (
    <Layout>
      <Events />
    </Layout>
  );
};

export default EventsPage;
